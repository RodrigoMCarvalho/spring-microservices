spring:
  application:
    name: registry
  server:
    port: 8081

eureka:
  client:
    register-with-eureka: false
    fetch-regstry: false
    service-url:
      defaultZone: hhtp://localhost:${server.port}/eureka/

